MAJOR = 0
MINOR = 0
MICRO = 15
ISRELEASED = False
VERSION = '{}.{}.{}'.format(MAJOR, MINOR, MICRO)
# print(VERSION)

def version():
    """Return Omin version as string

    Returns
    -------
    FULLVERSION : str
    """
    FULLVERSION = VERSION
    return FULLVERSION